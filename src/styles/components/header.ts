import { styled } from '..'

export const HeaderContainer = styled('header', {
  display: 'flex',
  justifyContent: 'space-between',
  width: '100%',
  maxWidth: 1180,
  margin: '0 auto',
  padding: '2rem 0rem',
})
